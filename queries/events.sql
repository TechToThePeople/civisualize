SELECT event.id, event.title, event.event_type_id, event.is_monetary, event.is_online_registration, event.start_date, event.end_date, COUNT(*) as count, participant.status_id, participant.register_date, fee_amount, SUM(fee_amount) from civicrm_event as event INNER JOIN civicrm_participant as participant ON event.id=participant.event_id group by event.id, status_id, participant.register_date;